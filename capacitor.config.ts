import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.appchckin',
  appName: 'appChckIn',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
